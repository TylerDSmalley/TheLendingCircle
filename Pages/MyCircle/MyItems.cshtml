@page
@using Microsoft.AspNetCore.Identity
@using TheLendingCircle.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@section Styles {
<link rel="stylesheet" href="~/css/MyCircle.css" />
}
@model TheLendingCircle.Pages.MyCircle.MyItems.MyItemsModel
@{
    ViewData["Title"] = "My Items";
}

<div class="container w-auto p-0">
    <div id="content" class="content p-0 w-100">
        <div class="profile-header w-100 p-0">
            <div class="profile-header-cover" style="background-color: #895ff0;"></div>
            <div class="profile-header-content">
                <div class="">
                    <img src="@Model.CurrentUser.UserPhotoPath" class="mb-4 rounded-circle cover float-start me-3"
                        alt="" />
                </div>

                <div class="ms-3">
                    <h4 class="mb-1">@Model.CurrentUser.FirstName @Model.CurrentUser.LastName</h4>
                    <p class="m-b-sm">@User.Identity?.Name</p>
                    <a class="btn btn-xs btn-primary mb-2" asp-area="Identity" asp-page="/Account/Manage/Index"
                        title="Manage">Edit Profile</a>
                </div>
            </div>

            <ul class="profile-header-tab nav nav-tabs">
                <li class="nav-item mt-1"><a href="./MyItems" class="nav-link active show" data-toggle="tab">MY
                        ITEMS</a></li>
                <li class="nav-item mt-1"><a href="./ClosedCircles" class="nav-link" data-toggle="tab">CLOSED
                        CIRCLES</a></li>
                <li class="nav-item mt-1"><a href="./OpenCircles" class="nav-link" data-toggle="tab">OPEN CIRCLES</a>
                </li>
                <li class="nav-item mt-1">
                    @if (@Model.UnseenRequests > 0)
                    {
                        <p class="float-start m-0 p-0 bg-danger text-white rounded-circle fs-6 fw-light"> &nbsp
                            @Model.UnseenRequests &nbsp </p>
                    }
                    <a href="./CircleRequests" class="nav-link" data-toggle="tab">CIRCLE REQUESTS</a>
                </li>
                <li class="nav-item mt-1"><a href="./MyReviews" class="nav-link" data-toggle="tab">MY REVIEWS</a></li>
            </ul>
        </div>

        <div class="container d-flex flex-column justify-content-evenly w-75">
            <div class="d-flex flex-row align-items-center mb-0 mt-2">
                <h2 class="mb-0">My Items in Circulation</h2>
                <p class="ms-3 mb-0">
                    <a class="btn btn-xs btn-primary my-2" asp-area="" asp-page="./AddItem">Add Item</a>
                </p>
            </div>
            <hr />

            @foreach (var item in Model.UserItems)
            {
                <div class="Container bg-white p-3 rounded-3 d-flex flex-column justify-content-evenly w-100">
                    <div class="row mx-4 my-2">
                        <div class="col col-md-7 mt-4">
                            <div class=""><b>My Item</b></div>
                            @* //Put image filepath here *@
                            <img src="@item.ItemPhotoPath" class="img-fluid mb-2" alt="" />
                            <a class="btn btn-primary" asp-page="./ItemPhotoUpload" asp-route-id="@item.Id">Upload new
                                image</a>
                        </div>
                        <div class="col-md-5 hidden-xs hidden-sm">
                            <ul class="profile-info-list">
                                <li class="title">ITEM INFORMATION</li>
                                <li>
                                    <div class="field">Id:</div>
                                    <div class="value">@item.Id</div>
                                </li>
                                <li>
                                    <div class="field">Title:</div>
                                    <div class="value">@item.Title</div>
                                </li>
                                <li>
                                    <div class="field">Description:</div>
                                    <div class="value">@item.Description</div>
                                </li>
                                <li>
                                    <div class="field">Condition:</div>
                                    <div class="value">@item.Condition</div>
                                </li>
                                <li>
                                    <div class="field">Available:</div>
                                    <div class="value">@item.Available</div>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
                <hr />
            }
        </div>
    </div>
</div>
